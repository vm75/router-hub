server {
    listen 80;
    server_name {{server_name}};

    include snippets/proxy-params.conf;

    location / {
        proxy_pass http://127.0.0.1:8080;
    }

    include domains-available/{{domain}}/subfolders-enabled/*.conf;
}
